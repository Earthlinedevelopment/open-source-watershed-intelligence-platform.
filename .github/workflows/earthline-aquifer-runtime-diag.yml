name: Earthline Texas Selector Fate Diagnostic

on:
  push:
    branches: [diag-aquifer-east-texas]
    paths:
      - 'tests/texas-selector-fate-diag.mjs'
      - '.github/workflows/earthline-aquifer-runtime-diag.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  texas-selector-fate-diag:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Playwright
        run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium
      - name: Run Texas selector fate diagnostic
        run: node tests/texas-selector-fate-diag.mjs
