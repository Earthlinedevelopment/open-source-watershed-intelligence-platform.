name: Earthline Salida Regional Renderer AB

on:
  push:
    branches: [diag-aquifer-east-texas]
    paths:
      - 'tests/salida-regional-renderer-ab.mjs'
      - '.github/workflows/earthline-aquifer-runtime-diag.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  salida-regional-renderer-ab:
    runs-on: ubuntu-latest
    timeout-minutes: 18
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Playwright
        run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium
      - name: Run Salida renderer baseline vs repair
        run: node tests/salida-regional-renderer-ab.mjs
