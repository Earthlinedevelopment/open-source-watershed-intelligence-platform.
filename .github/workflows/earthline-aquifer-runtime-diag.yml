name: Earthline Texas Panhandle Border Diagnostic

on:
  push:
    branches: [diag-aquifer-east-texas]
    paths:
      - 'tests/texas-panhandle-border-diag.mjs'
      - '.github/workflows/earthline-aquifer-runtime-diag.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  texas-panhandle-border-diag:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Playwright
        run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium
      - name: Run Texas panhandle border diagnostic
        run: node tests/texas-panhandle-border-diag.mjs
