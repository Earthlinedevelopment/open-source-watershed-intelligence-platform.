name: Mantra 46 16792 Local Low-Relief A-B
on:
  push:
    branches: [pages-live]
    paths:
      - 'tests/mantra46-16792-local-relaxed-ab.mjs'
      - '.github/workflows/earthline-m46-16792-local-relaxed.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  local-relaxed:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Chromium
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Run local low-relief A-B
        run: node tests/mantra46-16792-local-relaxed-ab.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mantra46-16792-local-relaxed
          path: artifacts/mantra46-16792-local-relaxed/
          if-no-files-found: error
