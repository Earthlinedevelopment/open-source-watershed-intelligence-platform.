name: Earthline 16748 Anchored Coverage 50-State Sweep
on:
  push:
    branches: [main]
    paths: ['.github/workflows/earthline-coverage50-16748.yml']
permissions:
  contents: read
jobs:
  sweep:
    strategy:
      fail-fast: false
      matrix:
        include:
          - batch: b1
            states: "Alabama|Alaska|Arizona|Arkansas|California|Colorado|Connecticut|Delaware|Florida|Georgia"
          - batch: b2
            states: "Hawaii|Idaho|Illinois|Indiana|Iowa|Kansas|Kentucky|Louisiana|Maine|Maryland"
          - batch: b3
            states: "Massachusetts|Michigan|Minnesota|Mississippi|Missouri|Montana|Nebraska|Nevada|New Hampshire|New Jersey"
          - batch: b4
            states: "New Mexico|New York|North Carolina|North Dakota|Ohio|Oklahoma|Oregon|Pennsylvania|Rhode Island|South Carolina"
          - batch: b5
            states: "South Dakota|Tennessee|Texas|Utah|Vermont|Virginia|Washington|West Virginia|Wisconsin|Wyoming"
    runs-on: ubuntu-24.04
    timeout-minutes: 24
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Chromium
        run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium
      - name: Run 10-state anchored coverage batch
        shell: bash
        run: |
          set +e
          EARTHLINE_URL='https://earthlinedevelopment.org/' STATES='${{ matrix.states }}' OUT='coverage-${{ matrix.batch }}.json' node lab/validate-16731-coverage-gap.mjs
          code=$?
          echo "$code" > "coverage-${{ matrix.batch }}.exit"
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: "coverage-${{ matrix.batch }}"
          path: |
            coverage-${{ matrix.batch }}.json
            coverage-${{ matrix.batch }}.exit
          if-no-files-found: error
