name: Mantra 46 16792 State Camera A-B

on:
  push:
    branches: [pages-live]
    paths:
      - 'tests/mantra46-16792-state-camera-ab.mjs'
      - '.github/workflows/earthline-m46-16792-state-camera-ab.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  state-camera:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Playwright
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Run state camera A-B
        run: node tests/mantra46-16792-state-camera-ab.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mantra46-16792-state-camera-ab
          path: artifacts/mantra46-16792-state-camera-ab/
          if-no-files-found: error
