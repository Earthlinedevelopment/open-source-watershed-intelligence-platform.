name: Earthline Texas Final Renderer Cull Test
on:
  push:
    branches: [diag-aquifer-east-texas]
    paths:
      - 'tests/texas-final-render-cull-test.mjs'
      - '.github/workflows/earthline-texas-final-render-cull.yml'
  workflow_dispatch:
permissions: {contents: read}
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 18
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: '20'}
      - run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium
      - run: node tests/texas-final-render-cull-test.mjs
