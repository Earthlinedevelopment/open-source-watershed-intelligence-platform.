name: Mantra 46 16787 Terrain A-B

on:
  push:
    branches: [pages-live]
    paths:
      - 'tests/mantra46-16787-terrain-budget-ab.mjs'
      - '.github/workflows/earthline-m46-16787-terrain-ab.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  terrain-ab:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Playwright
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Run efficient terrain recovery A-B
        run: node tests/mantra46-16787-terrain-budget-ab.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mantra46-16787-terrain-ab
          path: artifacts/mantra46-16787-terrain-ab/
          if-no-files-found: warn
