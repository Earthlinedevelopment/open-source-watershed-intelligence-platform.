name: Earthline Regional Renderer Zero Trace
on:
  push:
    branches: [diag-aquifer-east-texas]
    paths:
      - 'tests/trace-regional-renderer-zero.py'
      - '.github/workflows/earthline-renderer-zero-trace.yml'
  workflow_dispatch:
permissions: {contents: read}
jobs:
  trace:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - run: python tests/trace-regional-renderer-zero.py
