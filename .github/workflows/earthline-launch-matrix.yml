name: Earthline Jurisdiction-First Texas Gate

on:
  push:
    branches: [pages-live]
    paths:
      - 'tests/texas-ny-regression.mjs'
      - '.github/workflows/earthline-launch-matrix.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  jurisdiction-first-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Playwright
        run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium
      - name: Run Texas R-R-R plus New York and Vermont controls
        run: node tests/texas-ny-regression.mjs
      - name: Upload gate evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jurisdiction-first-texas-results
          path: tx-ny-regression-results.json
          if-no-files-found: error
          retention-days: 1
