name: Earthline Texas Shared Containment Trace

on:
  push:
    branches: [pages-live]
    paths:
      - 'tests/extract-texas-publication-owner.py'
      - '.github/workflows/earthline-launch-matrix.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trace-containment-owner:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - uses: actions/checkout@v4
      - name: Extract shared containment owner
        run: python tests/extract-texas-publication-owner.py
      - name: Upload containment trace
        uses: actions/upload-artifact@v4
        with:
          name: texas-containment-owner-trace
          path: texas-containment-owner-extract.txt
          if-no-files-found: error
          retention-days: 1
