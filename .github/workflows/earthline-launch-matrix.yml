name: Earthline Shared Terrain Owner Trace

on:
  push:
    branches: [pages-live]
    paths:
      - 'tests/extract-texas-publication-owner.py'
      - '.github/workflows/earthline-launch-matrix.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trace-terrain-owner:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - uses: actions/checkout@v4
      - name: Extract shared terrain owner
        run: python tests/extract-texas-publication-owner.py
      - name: Upload terrain owner trace
        uses: actions/upload-artifact@v4
        with:
          name: shared-terrain-owner-trace
          path: texas-publication-owner-extract.txt
          if-no-files-found: error
          retention-days: 1
