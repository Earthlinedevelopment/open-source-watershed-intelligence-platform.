name: Mantra 46 16792 Iowa Arkansas Live Visual

on:
  push:
    branches: [pages-live]
    paths:
      - 'tests/mantra46-16792-ia-ar-live-visual.mjs'
      - '.github/workflows/earthline-m46-16792-ia-ar-live-visual.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Playwright
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Capture deployed 16792 Iowa Arkansas
        run: node tests/mantra46-16792-ia-ar-live-visual.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mantra46-16792-ia-ar-live-visual
          path: artifacts/mantra46-16792-ia-ar-visual/
          if-no-files-found: error
