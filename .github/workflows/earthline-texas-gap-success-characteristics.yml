name: Earthline Texas Gap Success Characteristics
on:
  push:
    branches: [diag-aquifer-east-texas]
    paths:
      - 'tests/texas-gap-success-characteristics.mjs'
      - '.github/workflows/earthline-texas-gap-success-characteristics.yml'
  workflow_dispatch:
permissions: {contents: read}
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: '20'}
      - run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium
      - run: node tests/texas-gap-success-characteristics.mjs
