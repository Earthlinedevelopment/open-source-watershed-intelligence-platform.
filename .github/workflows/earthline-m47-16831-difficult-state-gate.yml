name: Mantra 47 16831 Difficult State Gate

on:
  push:
    branches: [diagnostic-m47-16822-ar-pipeline]
    paths:
      - 'tests/mantra47-16831-difficult-state-gate.mjs'
      - '.github/workflows/earthline-m47-16831-difficult-state-gate.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Playwright
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Run 16831 difficult-state gate
        run: node tests/mantra47-16831-difficult-state-gate.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mantra47-16831-difficult-state-gate
          path: artifacts/mantra47-16831-difficult-state-gate/
          if-no-files-found: error
