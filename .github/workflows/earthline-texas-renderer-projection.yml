name: Earthline Texas Renderer Projection State
on:
  push:
    branches: [diag-aquifer-east-texas]
    paths:
      - 'tests/texas-renderer-projection-state.mjs'
      - '.github/workflows/earthline-texas-renderer-projection.yml'
  workflow_dispatch:
permissions: {contents: read}
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: '20'}
      - run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium
      - run: node tests/texas-renderer-projection-state.mjs
