name: Earthline Renderer Lines 16751
on:
  push:
    branches: [main]
    paths: ['.github/workflows/earthline-renderer-lines-16751.yml']
permissions:
  contents: read
jobs:
  inspect:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with: {ref: pages-live}
      - run: sed -n '34320,34470p' index.html
