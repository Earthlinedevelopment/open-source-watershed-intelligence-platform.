name: Mantra 46 Final Regional Camera A-B

on:
  push:
    branches: [pages-live]
    paths:
      - 'tests/mantra46-camera-final-ab.mjs'
      - '.github/workflows/earthline-m46-camera-final-ab.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  camera-ab:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Playwright
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Run final camera A-B
        run: node tests/mantra46-camera-final-ab.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mantra46-camera-final-ab
          path: artifacts/mantra46-camera-final-ab/
          if-no-files-found: warn
