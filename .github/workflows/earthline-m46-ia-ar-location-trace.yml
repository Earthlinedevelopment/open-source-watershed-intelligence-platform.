name: Mantra 46 Iowa Arkansas Location Trace

on:
  push:
    branches: [pages-live]
    paths:
      - 'tests/mantra46-ia-ar-location-trace.mjs'
      - '.github/workflows/earthline-m46-ia-ar-location-trace.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trace:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Playwright
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Trace Iowa Arkansas live location lifecycle
        run: node tests/mantra46-ia-ar-location-trace.mjs
