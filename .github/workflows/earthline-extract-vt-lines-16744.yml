name: Earthline Vermont Owner Lines 16744
on:
  push:
    branches: [main]
    paths: ['.github/workflows/earthline-extract-vt-lines-16744.yml']
permissions:
  contents: read
jobs:
  extract:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: pages-live
      - run: sed -n '35870,36170p' index.html
