name: Earthline Run 16622 Repair

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/earthline-run-16621.yml'

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: pages-live
          fetch-depth: 0
      - name: Apply 16622
        run: python3 lab/apply-16622.py
      - name: Commit 16622
        run: |
          git diff --check
          git config user.name 'earthline-ci'
          git config user.email 'actions@users.noreply.github.com'
          if git diff --quiet -- index.html; then exit 0; fi
          git add index.html
          git commit -m 'fix: keep regional swales local to contour geometry'
          git push origin HEAD:pages-live
