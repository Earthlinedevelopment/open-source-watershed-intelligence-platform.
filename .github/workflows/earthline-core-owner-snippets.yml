name: Earthline Core Owner Snippets

on:
  push:
    branches: [pages-live]
    paths:
      - '.github/workflows/earthline-core-owner-snippets.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: pages-live
          fetch-depth: 0
      - name: Apply guarded sparse-land coverage repair
        run: |
          python3 lab/patch-us-sparse-land-coverage-16611.py
          git diff --check
          git diff --numstat -- index.html
          git diff -- index.html | sed -n '1,160p'
      - name: Commit product repair
        run: |
          git config user.name 'earthline-ci'
          git config user.email 'actions@users.noreply.github.com'
          git add index.html
          git commit -m 'fix: resample sparse US state land coverage'
          git push origin HEAD:pages-live
