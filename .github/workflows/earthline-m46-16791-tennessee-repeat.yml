name: Mantra 46 16791 Tennessee Repeat Gate

on:
  push:
    branches: [pages-live]
    paths:
      - 'tests/mantra46-16791-tennessee-repeat.mjs'
      - '.github/workflows/earthline-m46-16791-tennessee-repeat.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tennessee-repeat:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Playwright
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Run three Tennessee repeats
        run: node tests/mantra46-16791-tennessee-repeat.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mantra46-16791-tennessee-repeat
          path: artifacts/mantra46-16791-tennessee-repeat/
          if-no-files-found: warn
