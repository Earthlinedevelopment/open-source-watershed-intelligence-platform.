name: Earthline Renderer Projection Helper Trace
on:
  push:
    branches: [diag-aquifer-east-texas]
    paths:
      - 'tests/trace-renderer-projection-helpers.py'
      - '.github/workflows/earthline-renderer-projection-helpers.yml'
  workflow_dispatch:
permissions: {contents: read}
jobs:
  trace:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - run: python tests/trace-renderer-projection-helpers.py
