name: Earthline Prepared Jurisdiction Trace
on:
  push:
    branches: [diag-aquifer-east-texas]
    paths:
      - 'tests/trace-prepared-jurisdiction.py'
      - '.github/workflows/earthline-prepared-jurisdiction.yml'
  workflow_dispatch:
permissions: {contents: read}
jobs:
  trace:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - run: python tests/trace-prepared-jurisdiction.py
