name: Earthline Renderer Tail 16752
on:
  push:
    branches: [main]
    paths: ['.github/workflows/earthline-renderer-tail-16752.yml']
permissions:
  contents: read
jobs:
  inspect:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with: {ref: pages-live}
      - run: sed -n '34420,34530p' index.html
