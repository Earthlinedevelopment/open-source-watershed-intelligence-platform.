name: Earthline Swale Coverage Narrow Trace
on:
  push:
    branches: [diag-aquifer-east-texas]
    paths: ['tests/trace-swale-coverage-narrow.py','.github/workflows/earthline-swale-coverage-narrow.yml']
  workflow_dispatch:
permissions: {contents: read}
jobs:
  trace:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - run: python tests/trace-swale-coverage-narrow.py
