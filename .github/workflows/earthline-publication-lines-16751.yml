name: Earthline Publication Lines 16751
on:
  push:
    branches: [main]
    paths: ['.github/workflows/earthline-publication-lines-16751.yml']
permissions:
  contents: read
jobs:
  inspect:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with: {ref: pages-live}
      - run: sed -n '31720,31860p' index.html
