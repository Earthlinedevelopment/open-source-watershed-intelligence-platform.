import { chromium } from 'playwright';
import { mkdirSync, writeFileSync } from 'node:fs';
const BASE='https://earthlinedevelopment.org/',OUT='artifacts/mantra47-16829-opportunity-allocation-audit';mkdirSync(OUT,{recursive:true});
const browser=await chromium.launch({headless:true});const context=await browser.newContext({viewport:{width:1800,height:950}});
await context.route('https://earthlinedevelopment.org/**',async route=>{if(route.request().resourceType()!=='document')return route.continue();const resp=await route.fetch();let body=await resp.text();const capture='window.EARTHLINE_SWALE_GENERATION_AUDIT_16167=generationAudit;';if(body.split(capture).length-1!==1)throw new Error('capture mismatch');body=body.replace(capture,"window.__EARTHLINE_M47_16829={hy,candidates,chosen};"+capture);await route.fulfill({response:resp,body,headers:{...resp.headers(),'content-type':'text/html; charset=utf-8'}});});
const page=await context.newPage();const pageErrors=[];page.on('pageerror',e=>pageErrors.push(String(e)));let loadError=null,timedOut=false;const started=Date.now();
try{await page.goto(BASE+'?m47_16829='+Date.now(),{waitUntil:'domcontentloaded',timeout:45000});await page.waitForSelector('#searchInput',{timeout:30000});await page.evaluate(()=>{const i=document.getElementById('searchInput'),b=document.getElementById('runBtn');i.value='Arkansas';i.dispatchEvent(new Event('input',{bubbles:true}));i.dispatchEvent(new Event('change',{bubbles:true}));b.click();});try{await page.waitForFunction(()=>{const s=String(document.getElementById('earthlineVermontStatus16147')?.textContent||document.getElementById('earthlineTierNotice16173')?.textContent||'');return /screening published\./i.test(s)&&!!window.__EARTHLINE_M47_16829;},null,{timeout:65000,polling:100});}catch(_){timedOut=true;}await page.waitForTimeout(800);}catch(e){loadError=String(e);}
const audit=loadError||timedOut?null:await page.evaluate(()=>{const {hy,candidates,chosen}=window.__EARTHLINE_M47_16829,N=12;const vals=Array.from(hy.acc||[]).map(Number).filter(Number.isFinite).sort((a,b)=>a-b),channel=vals.length?vals[Math.max(0,Math.min(vals.length-1,Math.floor((vals.length-1)*.972)))]:Infinity;const cellOf=c=>{const gx=Number.isFinite(Number(c?.coverageGX16775))?Number(c.coverageGX16775):Number(c?.x),gy=Number.isFinite(Number(c?.coverageGY16775))?Number(c.coverageGY16775):Number(c?.y);if(!Number.isFinite(gx)||!Number.isFinite(gy))return null;return [Math.max(0,Math.min(N-1,Math.floor(gx*N/Math.max(1,hy.w)))),Math.max(0,Math.min(N-1,Math.floor(gy*N/Math.max(1,hy.h))))];};const cand=new Map(),sel=new Map();for(const c of candidates){const p=cellOf(c);if(p){const k=p.join(',');cand.set(k,(cand.get(k)||0)+1);}}for(const c of chosen){const p=cellOf(c);if(p){const k=p.join(',');sel.set(k,(sel.get(k)||0)+1);}}const rows=[];for(let by=0;by<N;by++)for(let bx=0;bx<N;bx++){let valid=0,opp=0,pref=0,channels=0;const x0=Math.floor(bx*hy.w/N),x1=Math.min(hy.w,Math.ceil((bx+1)*hy.w/N)),y0=Math.floor(by*hy.h/N),y1=Math.min(hy.h,Math.ceil((by+1)*hy.h/N));for(let y=y0;y<y1;y++)for(let x=x0;x<x1;x++){const i=y*hy.w+x;if(hy.validityMask16584?.[i]!==1)continue;valid++;const sp=Number(hy.slope[i]),ac=Number(hy.acc[i]);if(!Number.isFinite(sp)||!Number.isFinite(ac))continue;if(ac>=channel){channels++;continue;}if(sp>=.05&&sp<=4){opp++;if(sp>=.25&&sp<=3.5)pref++;}}const k=bx+','+by;rows.push({bx,by,valid,opp,pref,channels,oppRatio:valid?opp/valid:0,prefRatio:valid?pref/valid:0,candidates:cand.get(k)||0,chosen:sel.get(k)||0});}const withOpp=rows.filter(r=>r.opp>0).sort((a,b)=>b.oppRatio-a.oppRatio||b.opp-a.opp);withOpp.forEach((r,i)=>r.oppRank=i+1);const target=rows.find(r=>r.bx===6&&r.by===6);return {grid:[hy.w,hy.h],channel,totalCandidates:candidates.length,totalChosen:chosen.length,target,rows,topOpportunity:withOpp.slice(0,30),underselected:rows.filter(r=>r.candidates>0).sort((a,b)=>(b.oppRatio/(b.chosen+1))-(a.oppRatio/(a.chosen+1))).slice(0,30)};});
const result={loadError,timedOut,pageErrors,elapsedMs:Date.now()-started,audit};writeFileSync(OUT+'/results.json',JSON.stringify(result,null,2));console.log('EARTHLINE_M47_16829 '+JSON.stringify(result));try{await page.screenshot({path:OUT+'/arkansas.png',fullPage:false});}catch(_){}await browser.close();if(loadError||timedOut||pageErrors.length||!audit)process.exitCode=1;
