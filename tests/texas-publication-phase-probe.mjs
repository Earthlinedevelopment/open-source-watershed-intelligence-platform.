import { chromium } from 'playwright';
import { writeFileSync } from 'node:fs';

const BASE='https://earthlinedevelopment.org/';
const browser=await chromium.launch({headless:true});
const page=await browser.newPage({viewport:{width:1800,height:950}});
const pageErrors=[]; page.on('pageerror',e=>pageErrors.push(String(e)));
const patch={};
function replaceOne(body,name,needle,replacement){const n=body.split(needle).length-1;patch[name]=n;if(n!==1)throw new Error(name+' expected once, found '+n);return body.replace(needle,replacement);}

await page.route('**/*',async route=>{
  const req=route.request();
  if(req.isNavigationRequest()&&req.resourceType()==='document'&&req.url().startsWith(BASE)){
    const response=await route.fetch(); let body=await response.text();
    body=replaceOne(body,'spacingPrimary','const chosen=[],primarySpacing=Math.max(6,Math.round(hy.w/31));','const chosen=[],primarySpacing=Math.max(3,Math.round(hy.w/31));');
    body=replaceOne(body,'spacingSecondary','const spacing=chosen.length<20?primarySpacing:Math.max(4,Math.round(primarySpacing*.72));','const spacing=chosen.length<20?primarySpacing:Math.max(2,Math.round(primarySpacing*.72));');
    body=replaceOne(body,'probeStart','    const productsStarted16198=performance.now();','    const productsStarted16198=performance.now();window.EARTHLINE_TX_PUBLICATION_PROBE_16593={runToken,started:productsStarted16198};');
    body=replaceOne(body,'contours',"    let contours=await makeContours(hy);noteRegionalProgress16347('DERIVING WATER PATHS + BIOSWALES · CONTOURS COMPLETE',runToken);await wait(0);","    const txContourStart16593=performance.now();let contours=await makeContours(hy);window.EARTHLINE_TX_PUBLICATION_PROBE_16593.contoursMs=Math.round(performance.now()-txContourStart16593);noteRegionalProgress16347('DERIVING WATER PATHS + BIOSWALES · CONTOURS COMPLETE',runToken);await wait(0);");
    body=replaceOne(body,'boundaryAwait','      selectionBoundary16539=await jurisdictionBoundaryPromise16539;','      const txBoundaryAwaitStart16593=performance.now();selectionBoundary16539=await jurisdictionBoundaryPromise16539;window.EARTHLINE_TX_PUBLICATION_PROBE_16593.boundaryAwaitMs=Math.round(performance.now()-txBoundaryAwaitStart16593);');
    body=replaceOne(body,'swales',"    let swales=makeSwales(hy,contours,focusMode,swaleJurisdictionGeometry16539);noteRegionalProgress16347('DERIVING WATER PATHS + BIOSWALES · SWALES COMPLETE',runToken);await wait(0);","    const txSwalesStart16593=performance.now();let swales=makeSwales(hy,contours,focusMode,swaleJurisdictionGeometry16539);window.EARTHLINE_TX_PUBLICATION_PROBE_16593.makeSwalesMs=Math.round(performance.now()-txSwalesStart16593);noteRegionalProgress16347('DERIVING WATER PATHS + BIOSWALES · SWALES COMPLETE',runToken);await wait(0);");
    body=replaceOne(body,'styleWater','      hy.styleWaterIndex16584=earthlineLoadedStyleWaterIndex16584(map(),hy.bounds);','      const txStyleWaterStart16593=performance.now();hy.styleWaterIndex16584=earthlineLoadedStyleWaterIndex16584(map(),hy.bounds);window.EARTHLINE_TX_PUBLICATION_PROBE_16593.styleWaterIndexMs=Math.round(performance.now()-txStyleWaterStart16593);');
    body=replaceOne(body,'flows',"    let flows=makeFlows(hy);noteRegionalProgress16347('DERIVING WATER PATHS + BIOSWALES · FLOWS COMPLETE',runToken);await wait(0);","    const txFlowsStart16593=performance.now();let flows=makeFlows(hy);window.EARTHLINE_TX_PUBLICATION_PROBE_16593.makeFlowsMs=Math.round(performance.now()-txFlowsStart16593);noteRegionalProgress16347('DERIVING WATER PATHS + BIOSWALES · FLOWS COMPLETE',runToken);await wait(0);const txFlowAuditStart16593=performance.now();");
    body=replaceOne(body,'flowAuditEnd','    window.EARTHLINE_ORB_RESPONSIVENESS_16245={build:\'EARTHLINE 16245\',cooperativeTerrain:true,compositorOrb:true,productsMs:Math.round(performance.now()-contoursStarted16245),at:new Date().toISOString()};','    window.EARTHLINE_TX_PUBLICATION_PROBE_16593.flowAuditMs=Math.round(performance.now()-txFlowAuditStart16593);window.EARTHLINE_ORB_RESPONSIVENESS_16245={build:\'EARTHLINE 16245\',cooperativeTerrain:true,compositorOrb:true,productsMs:Math.round(performance.now()-contoursStarted16245),at:new Date().toISOString()};');
    body=replaceOne(body,'genericClip','      const bounded16539=earthlineClipRegionalProducts16539({contours,flows,swales,query:q,runToken},boundary16539);','      const txBoundaryClipStart16593=performance.now();const bounded16539=earthlineClipRegionalProducts16539({contours,flows,swales,query:q,runToken},boundary16539);window.EARTHLINE_TX_PUBLICATION_PROBE_16593.boundaryClipMs=Math.round(performance.now()-txBoundaryClipStart16593);');
    body=replaceOne(body,'cameraSettle','    const cameraReady16334=await cameraSettle16310;','    const cameraReady16334=await cameraSettle16310;window.EARTHLINE_TX_PUBLICATION_PROBE_16593.cameraSettleMs=Math.round(performance.now()-cameraFinalStarted16329);');
    body=replaceOne(body,'finalWater','      flows=earthlineFinalWaterClip16584(hy,flows,m,runToken);','      const txFinalWaterStart16593=performance.now();flows=earthlineFinalWaterClip16584(hy,flows,m,runToken);window.EARTHLINE_TX_PUBLICATION_PROBE_16593.finalWaterClipMs=Math.round(performance.now()-txFinalWaterStart16593);');
    body=replaceOne(body,'sourceStart',"    if(!guardedSetGeo(runToken,m,IDS.contours,contours,'earthline-modeled-contours'))return false;","    const txSourceStart16593=performance.now();if(!guardedSetGeo(runToken,m,IDS.contours,contours,'earthline-modeled-contours'))return false;");
    body=replaceOne(body,'sourceEnd',"    if(!guardedSetGeo(runToken,m,IDS.swales,swales,focusMode?'earthline-focus-opportunity-corridors':'earthline-regional-opportunity-corridors'))return false;","    if(!guardedSetGeo(runToken,m,IDS.swales,swales,focusMode?'earthline-focus-opportunity-corridors':'earthline-regional-opportunity-corridors'))return false;window.EARTHLINE_TX_PUBLICATION_PROBE_16593.sourceSetMs=Math.round(performance.now()-txSourceStart16593);");
    body=replaceOne(body,'renderStart','    const regionalOverlayRenderOk16336=\n      typeof window.earthlineRenderRegionalOverlay16020===\'function\'\n        ?window.earthlineRenderRegionalOverlay16020(visualData)\n        :false;','    const txOverlayStart16593=performance.now();const regionalOverlayRenderOk16336=\n      typeof window.earthlineRenderRegionalOverlay16020===\'function\'\n        ?window.earthlineRenderRegionalOverlay16020(visualData)\n        :false;window.EARTHLINE_TX_PUBLICATION_PROBE_16593.overlayRenderMs=Math.round(performance.now()-txOverlayStart16593);');
    body=replaceOne(body,'layerStart','    for(const id of [IDS.contourLine,IDS.flowCasing,IDS.flowLine,IDS.contourLabel,IDS.flowArrow,IDS.swaleHalo,IDS.swaleLine,IDS.aquiferLabel]){try{m.moveLayer(id);}catch(_){}}','    const txLayerStart16593=performance.now();for(const id of [IDS.contourLine,IDS.flowCasing,IDS.flowLine,IDS.contourLabel,IDS.flowArrow,IDS.swaleHalo,IDS.swaleLine,IDS.aquiferLabel]){try{m.moveLayer(id);}catch(_){}}');
    body=replaceOne(body,'phaseEnd',"    phaseMark16198('terrainProductsAndPublishMs',productsStarted16198);","    try{m.triggerRepaint&&m.triggerRepaint();}catch(_){}window.EARTHLINE_TX_PUBLICATION_PROBE_16593.layerMoveRepaintMs=Math.round(performance.now()-txLayerStart16593);window.EARTHLINE_TX_PUBLICATION_PROBE_16593.totalMs=Math.round(performance.now()-productsStarted16198);phaseMark16198('terrainProductsAndPublishMs',productsStarted16198);");
    await route.fulfill({response,body}); return;
  }
  await route.continue();
});

let loadError=null; const results=[];
try{await page.goto(BASE+'?earthline_tx_pub_probe='+Date.now(),{waitUntil:'domcontentloaded',timeout:45000});await page.waitForSelector('#searchInput',{timeout:30000});}catch(e){loadError=String(e);}
for(let run=0;run<3&&!loadError;run++){
  const prev=await page.evaluate(()=>window.EARTHLINE_LAND_VALIDITY_FLOW_AUDIT_16584?.runToken||window.EARTHLINE_LAST_LIVE_REGIONAL_ERROR_15970?.runToken||null);
  const started=Date.now(); let timedOut=false;
  await page.evaluate(()=>{const i=document.getElementById('searchInput'),b=document.getElementById('runBtn');i.focus();i.value='Texas';i.dispatchEvent(new Event('input',{bubbles:true}));i.dispatchEvent(new Event('change',{bubbles:true}));b.click();});
  try{await page.waitForFunction(prev=>{const f=window.EARTHLINE_LAND_VALIDITY_FLOW_AUDIT_16584||null,e=window.EARTHLINE_LAST_LIVE_REGIONAL_ERROR_15970||null,token=(e&&e.runToken)||(f&&f.runToken)||null;if(!token||token===prev)return false;if(e&&e.runToken===token)return true;const s=String(document.getElementById('earthlineVermontStatus16147')?.textContent||document.getElementById('earthlineTierNotice16173')?.textContent||'');return /screening published\./i.test(s)&&!!window.EARTHLINE_REGIONAL_PERFORMANCE_16191;},prev,{timeout:35000,polling:50});}catch(_){timedOut=true;}
  const a=await page.evaluate(()=>({probe:window.EARTHLINE_TX_PUBLICATION_PROBE_16593||null,perf:window.EARTHLINE_REGIONAL_PERFORMANCE_16191||null,generation:window.EARTHLINE_SWALE_GENERATION_AUDIT_16167||null,pub:window.EARTHLINE_CORRIDOR_PUBLICATION_AUDIT_16167||null,display:window.EARTHLINE_REGIONAL_DISPLAY_AUDIT_16040||null,flow:window.EARTHLINE_LAND_VALIDITY_FLOW_AUDIT_16584||null,lastError:window.EARTHLINE_LAST_LIVE_REGIONAL_ERROR_15970||null}));
  const r={run:run+1,clickToTerminalMs:Date.now()-started,timedOut,coreMs:a?.perf?.totalMs??null,phases:a?.perf?.phaseTotalsMs??null,probe:a?.probe||null,candidates:a?.generation?.candidates??null,eligible:a?.generation?.jurisdictionEligibleCandidates??null,chosen:a?.generation?.chosenBeforeTierGate??null,published:a?.pub?.generated??null,visible:a?.display?.swaleLines??null,gridW:a?.flow?.gridAudit?.grid?.w??null,gridH:a?.flow?.gridAudit?.grid?.h??null,unsafe:a?.flow?.unsafeSegments??null,lastError:a?.lastError||null,pageErrors:pageErrors.slice()};
  results.push(r); console.log('EARTHLINE_TX_PUB_PHASE '+JSON.stringify(r));
}
await browser.close();writeFileSync('texas-publication-phase-probe.json',JSON.stringify({loadError,patch,results},null,2));
if(loadError||Object.values(patch).some(v=>v!==1)||results.length!==3||results.some(r=>r.timedOut||r.lastError||r.pageErrors.length||Number(r.gridW)!==96||Number(r.gridH)!==96||Number(r.unsafe)!==0||Number(r.published)<40||Number(r.visible)<40))process.exitCode=1;
