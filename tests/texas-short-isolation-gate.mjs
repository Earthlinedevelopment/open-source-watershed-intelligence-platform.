import { chromium } from 'playwright';
const URL='https://earthlinedevelopment.org/';
const browser=await chromium.launch({headless:true});
const page=await browser.newPage({viewport:{width:1800,height:1000}});
const errors=[];page.on('pageerror',e=>errors.push(String(e)));page.on('console',m=>{if(m.type()==='error')errors.push(m.text());});
const patches={};
await page.route('**/*',async route=>{
 if(route.request().resourceType()!=='document')return route.continue();
 const resp=await route.fetch();let body=await resp.text();
 const apply=(name,n,r)=>{patches[name]=body.split(n).length-1;body=body.split(n).join(r);};
 apply('landMasksInit','const valid16584=new Uint8Array(w16584*h16584);','const valid16584=new Uint8Array(w16584*h16584),outsideLandMask16630=new Uint8Array(w16584*h16584),inlandWaterMask16630=new Uint8Array(w16584*h16584);');
 apply('outsideLandMask','if(!onLand16584){outsideLand16584++;continue;}','if(!onLand16584){outsideLand16584++;outsideLandMask16630[y16584*w16584+x16584]=1;continue;}');
 apply('inlandWaterMask','if(inWater16584){inlandWater16584++;continue;}','if(inWater16584){inlandWater16584++;inlandWaterMask16630[y16584*w16584+x16584]=1;continue;}');
 apply('landMasksReturn','return {mask:valid16584,audit:audit16584};','return {mask:valid16584,audit:audit16584,outsideLandMask16630,inlandWaterMask16630};');
 apply('hyLandMasks',"hy=await hydrology(dem,validityGrid16584.mask);\n          hy.waterParts16584=Array.isArray(landValidity16584.waterParts)?landValidity16584.waterParts:[];","hy=await hydrology(dem,validityGrid16584.mask);\n          hy.waterParts16584=Array.isArray(landValidity16584.waterParts)?landValidity16584.waterParts:[];\n          hy.outsideLandMask16630=validityGrid16584.outsideLandMask16630||null;\n          hy.inlandWaterMask16630=validityGrid16584.inlandWaterMask16630||null;");
 apply('sampleInit','function sampleSegment(coords,center,relaxed){',"window.EARTHLINE_SINGLE_PASS_GAP_16630={edgeQualified:0,coastQualified:0,shortRejected:0};\n    function sampleSegment(coords,center,relaxed){");
 apply('sampleLength','const segment=chaikin(raw,2,false);if(segment.length<10||lineLengthPixels(hy,segment)<10)return null;',"const segment=chaikin(raw,2,false);if(segment.length<10)return null;\n      const linePx16630=lineLengthPixels(hy,segment);let minLinePx16630=10,shortClass16630=null;\n      if(!focusMode&&linePx16630<10&&linePx16630>=4&&Math.max(Number(hy.cellX)||0,Number(hy.cellY)||0)>10000){\n        const mp16630=segment[Math.floor((segment.length-1)/2)],mg16630=llGrid(hy,mp16630);\n        if(mg16630&&Number.isFinite(mg16630.x)&&Number.isFinite(mg16630.y)){\n          const edge16630=Math.min(Number(mg16630.y),Math.max(0,(Number(hy.h)-1)-Number(mg16630.y)));\n          if(edge16630<=6){minLinePx16630=4;shortClass16630='edge';}\n          else if(hy.outsideLandMask16630){\n            const cx16630=Math.max(0,Math.min(hy.w-1,Math.round(Number(mg16630.x)))),cy16630=Math.max(0,Math.min(hy.h-1,Math.round(Number(mg16630.y))));\n            outer16630:for(let dy16630=-9;dy16630<=9;dy16630++)for(let dx16630=-9;dx16630<=9;dx16630++){const x16630=cx16630+dx16630,y16630=cy16630+dy16630;if(x16630<0||x16630>=hy.w||y16630<0||y16630>=hy.h)continue;if(hy.outsideLandMask16630[y16630*hy.w+x16630]){minLinePx16630=4;shortClass16630='coast';break outer16630;}}\n          }\n        }\n      }\n      if(linePx16630<minLinePx16630){if(linePx16630<10)window.EARTHLINE_SINGLE_PASS_GAP_16630.shortRejected++;return null;}");
 apply('sampleReturn',"return {segment,x:anchor.x,y:anchor.y,slope:meanSlope,acc:meanAcc,maxAcc,score:slopeScore*.56+flowScore*.28+lengthScore*.16,confidence:relaxed?'screening':'preferred'};","if(shortClass16630==='edge')window.EARTHLINE_SINGLE_PASS_GAP_16630.edgeQualified++;else if(shortClass16630==='coast')window.EARTHLINE_SINGLE_PASS_GAP_16630.coastQualified++;return {segment,x:anchor.x,y:anchor.y,slope:meanSlope,acc:meanAcc,maxAcc,score:slopeScore*.56+flowScore*.28+lengthScore*.16,confidence:relaxed?'screening':'preferred',minLinePx16630,shortClass16630};");
 apply('screenLength','if(!segment16539||segment16539.length<2||lineLengthPixels(hy,segment16539)<10){if(candidate16539)jurisdictionScreenCache16592.set(candidate16539,null);return null;}','if(!segment16539||segment16539.length<2||lineLengthPixels(hy,segment16539)<Math.max(4,Math.min(10,Number(candidate16539&&candidate16539.minLinePx16630)||10))){if(candidate16539)jurisdictionScreenCache16592.set(candidate16539,null);return null;}');
 apply('shortIsolation',"    for(const f of lines){\n      const coords=f.geometry.coordinates;if(coords.length<16)continue;\n      const fractions=coords.length>110?[.18,.36,.54,.72,.86]:coords.length>55?[.26,.52,.78]:[.38,.68];\n      for(const frac of fractions){const c=sampleSegment(coords,Math.max(3,Math.min(coords.length-4,Math.round((coords.length-1)*frac))),false);if(c)candidates.push(c);}\n    }","    const shortCandidates16638=[];\n    for(const f of lines){\n      const coords=f.geometry.coordinates;if(coords.length<16)continue;\n      const fractions=coords.length>110?[.18,.36,.54,.72,.86]:coords.length>55?[.26,.52,.78]:[.38,.68];\n      for(const frac of fractions){\n        const c=sampleSegment(coords,Math.max(3,Math.min(coords.length-4,Math.round((coords.length-1)*frac))),false);\n        if(c){if(c.shortClass16630)shortCandidates16638.push(c);else candidates.push(c);}\n      }\n    }\n    let shortAdmitted16638=0;\n    if(shortCandidates16638.length){\n      const covered16638=[];\n      for(const c16638 of candidates){const s16638=screenJurisdictionCandidate16539(c16638);if(s16638)covered16638.push(s16638);}\n      const rankedShort16638=shortCandidates16638.map(c16638=>{\n        let nearest16638=Infinity;\n        for(const q16638 of covered16638)nearest16638=Math.min(nearest16638,Math.hypot(Number(q16638.x)-Number(c16638.x),Number(q16638.y)-Number(c16638.y)));\n        return {c16638,nearest16638};\n      }).filter(x16638=>x16638.nearest16638>=3).sort((a16638,b16638)=>(b16638.nearest16638-a16638.nearest16638)||(b16638.c16638.score-a16638.c16638.score)).slice(0,12);\n      for(const item16638 of rankedShort16638){\n        const c16638=item16638.c16638,s16638=screenJurisdictionCandidate16539(c16638);if(!s16638)continue;\n        const spacing16638=c16638.shortClass16630==='edge'?6:3;\n        if(covered16638.some(q16638=>Math.hypot(Number(q16638.x)-Number(s16638.x),Number(q16638.y)-Number(s16638.y))<spacing16638))continue;\n        candidates.push(c16638);covered16638.push(s16638);shortAdmitted16638++;\n      }\n      window.EARTHLINE_SHORT_ISOLATION_16638={generated:shortCandidates16638.length,considered:rankedShort16638.length,admitted:shortAdmitted16638};\n    }");
 apply('preResize','    const cameraSettle16310=Promise.resolve(settleRegionalCamera(m,b,runToken)).catch(()=>false);','    try{m.resize&&m.resize();}catch(_){}\n    const cameraSettle16310=Promise.resolve(settleRegionalCamera(m,b,runToken)).catch(()=>false);');
 apply('noLateResize',"const resizeRunToken16020=String(lastData&&lastData.runToken||'');if(resizeRunToken16020&&resizeRunToken16020!==lastResizeRunToken16020){lastResizeRunToken16020=resizeRunToken16020;try{map.resize&&map.resize();}catch(_){}}","const resizeRunToken16020=String(lastData&&lastData.runToken||'');if(resizeRunToken16020&&resizeRunToken16020!==lastResizeRunToken16020){lastResizeRunToken16020=resizeRunToken16020;}");
 apply('rendererTrace',"    const regionalOverlayRenderOk16336=\n      typeof window.earthlineRenderRegionalOverlay16020==='function'\n        ?window.earthlineRenderRegionalOverlay16020(visualData)\n        :false;","    const rendererPre16632={mapWidth:Number(m&&m.getCanvas&&m.getCanvas()?.clientWidth||0),mapHeight:Number(m&&m.getCanvas&&m.getCanvas()?.clientHeight||0),overlay:(()=>{const e=document.getElementById('earthlineRegionalVectorOverlay16020');if(!e)return null;const r=e.getBoundingClientRect(),s=getComputedStyle(e);return {w:r.width,h:r.height,left:r.left,top:r.top,display:s.display,visibility:s.visibility,opacity:s.opacity};})(),zoom:Number(m&&m.getZoom?m.getZoom():0)};\n    const regionalOverlayRenderOk16336=typeof window.earthlineRenderRegionalOverlay16020==='function'?window.earthlineRenderRegionalOverlay16020(visualData):false;\n    const rendererPost16632={audit:window.EARTHLINE_REGIONAL_DISPLAY_AUDIT_16040||window.EARTHLINE_REGIONAL_DISPLAY_AUDIT_16020||null,overlay:(()=>{const e=document.getElementById('earthlineRegionalVectorOverlay16020');if(!e)return null;const r=e.getBoundingClientRect(),s=getComputedStyle(e);return {w:r.width,h:r.height,children:e.children.length,display:s.display,visibility:s.visibility,opacity:s.opacity};})()};\n    window.EARTHLINE_RENDERER_ZERO_TRACE_16632={pre:rendererPre16632,post:rendererPost16632,runToken,at:new Date().toISOString()};")
 return route.fulfill({response:resp,body});
});
await page.goto(URL+'?tx_local_gap='+Date.now(),{waitUntil:'domcontentloaded',timeout:45000});
await page.waitForSelector('#searchInput',{timeout:30000});
const rows=[];
for(let repeat=1;repeat<=3;repeat++){
 const prior=await page.evaluate(()=>String(window.EARTHLINE_SWALE_GENERATION_AUDIT_16167?.at||''));const started=Date.now();
 await page.evaluate(()=>{const i=document.getElementById('searchInput'),b=document.getElementById('runBtn');i.value='Texas';i.dispatchEvent(new Event('input',{bubbles:true}));i.dispatchEvent(new Event('change',{bubbles:true}));b.click();});
 let timedOut=false;try{await page.waitForFunction(prev=>{const at=String(window.EARTHLINE_SWALE_GENERATION_AUDIT_16167?.at||''),s=String(document.getElementById('earthlineVermontStatus16147')?.textContent||'');return !!window.EARTHLINE_LAST_LIVE_REGIONAL_ERROR_15970||((!prev||at!==prev)&&/screening published\./i.test(s));},prior,{timeout:30000,polling:100});}catch(_){timedOut=true;}
 await page.waitForTimeout(350);
 const state=await page.evaluate(()=>{
  const v=window.EARTHLINE_REGIONAL_VISUAL_DATA_16020||null,sw=Array.isArray(v?.swales?.features)?v.swales.features:[];
  const mid=f=>{const c=f?.geometry?.coordinates||[];return Array.isArray(c)&&c.length?c[Math.floor((c.length-1)/2)]:null;};
  const count=pred=>sw.reduce((n,f)=>{const m=mid(f);return n+(Array.isArray(m)&&pred(+m[0],+m[1])?1:0);},0);
  const p=window.EARTHLINE_REGIONAL_PERFORMANCE_16191||null,g=window.EARTHLINE_SWALE_GENERATION_AUDIT_16167||null,b=window.EARTHLINE_REGIONAL_JURISDICTION_BOUNDARY_AUDIT_16539||null,d=window.EARTHLINE_REGIONAL_DISPLAY_AUDIT_16040||null;
  return {shortIsolation:window.EARTHLINE_SHORT_ISOLATION_16638||null,singlePass:window.EARTHLINE_SINGLE_PASS_GAP_16630||null,rendererTrace:window.EARTHLINE_RENDERER_ZERO_TRACE_16632||null,swales:sw.length,visible:d?.swaleLines??null,zones:{panhandleNorth:count((x,y)=>x>-103.1&&x<-100&&y>35&&y<36.6),panhandleWestNM:count((x,y)=>x>-103.2&&x<-102&&y>31.8&&y<36.6),upperCoast:count((x,y)=>x>-96.5&&x<-93.45&&y>28.8&&y<31.2),midCoast:count((x,y)=>x>-99.3&&x<-96&&y>27.4&&y<30.2),lowerCoast:count((x,y)=>x>-99.5&&x<-97&&y>25.7&&y<28.2),eastInterior:count((x,y)=>x>-96&&x<-93.45&&y>30.5&&y<34.3)},candidates:g?.candidates??null,eligible:g?.jurisdictionEligibleCandidates??null,totalMs:p?.totalMs??null,phases:p?.phaseTotalsMs??null,outside:b?.outsideAfterClip??null,lastError:window.EARTHLINE_LAST_LIVE_REGIONAL_ERROR_15970||null};
 });
 const row={repeat,elapsedMs:Date.now()-started,timedOut,state};rows.push(row);console.log('EARTHLINE_TX_LOCAL_GAP '+JSON.stringify(row));
}
console.log('EARTHLINE_TX_LOCAL_GAP_SUMMARY '+JSON.stringify({patches,rows,errors:errors.slice(0,20)}));
const failed=rows.some(r=>{const s=r.state||{},z=s.zones||{},o=s.outside||{};return r.timedOut||!!s.lastError||Number(s.visible||0)!==Number(s.swales||0)||Number(s.visible||0)<=0||Number(z.panhandleNorth||0)<3||Number(z.upperCoast||0)<8||Number(z.lowerCoast||0)<5||Number(z.eastInterior||0)<10||Number(o.swales||0)!==0||!Number.isFinite(Number(s.totalMs))||Number(s.totalMs)>15000;});
console.log('EARTHLINE_TX_SHORT_ISOLATION_GATE '+JSON.stringify({passed:!failed,rows:rows.map(r=>({repeat:r.repeat,totalMs:r.state?.totalMs,visible:r.state?.visible,shortIsolation:r.state?.shortIsolation,zones:r.state?.zones,outside:r.state?.outside,lastError:r.state?.lastError}))}));
if(failed)process.exitCode=1;
await browser.close();
