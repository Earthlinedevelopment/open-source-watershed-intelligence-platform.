from pathlib import Path
p=Path('index.html')
s=p.read_text(encoding='utf-8')
old='''  let f=earthlineSelectPlaceCandidate16537(q,j.features);\n  if(!f)return null;\n  /* EARTHLINE 16846 — global country-name ambiguity fallback. If a short bare\n     query resolves to an identically named non-country result, ask the same\n     geocoder once for countries only. The country fallback is accepted only when\n     the country name is exact or has high relevance plus the same word count and\n     a shared substantive token. This admits close exonyms such as Cabo/Cape Verde\n     while rejecting unrelated low-relevance countries such as Chiang Mai/Mali.\n     Addresses, comma-qualified searches and ordinary qualified place searches\n     keep the existing single-query path. */\n  const bareCountryCheck16846=!/[0-9,@]/.test(String(q||''))&&earthlinePlaceKey16537(q).split(/\\s+/).filter(Boolean).length<=4;\n  const queryKey16846=earthlinePlaceKey16537(q),selectedType16846=earthlinePlaceType16537(f),selectedName16846=earthlinePlaceKey16537(f.text||String(f.place_name||'').split(',')[0]);\n  if(bareCountryCheck16846&&selectedType16846!=='country'&&selectedName16846===queryKey16846){\n    try{\n      const countryUrl16846="https://api.mapbox.com/geocoding/v5/mapbox.places/"+encodeURIComponent(earthlinePlaceLookupQuery16541(q))+".json?limit=5&types=country&language=en&access_token="+MAPBOX_TOKEN;\n      const countryResponse16846=await fetch(countryUrl16846);\n      if(earthlineIsStale(myGen))return null;\n      if(countryResponse16846.ok){\n        const countryJson16846=await countryResponse16846.json();\n        if(earthlineIsStale(myGen))return null;\n        const countryCandidate16846=earthlineSelectPlaceCandidate16537(q,countryJson16846&&countryJson16846.features||[]);\n        if(countryCandidate16846&&earthlinePlaceType16537(countryCandidate16846)==='country'){\n          const countryName16846=earthlinePlaceKey16537(countryCandidate16846.text||String(countryCandidate16846.place_name||'').split(',')[0]);\n          const queryTokens16846=queryKey16846.split(/\\s+/).filter(Boolean),countryTokens16846=countryName16846.split(/\\s+/).filter(Boolean);\n          const sharedSubstantive16846=queryTokens16846.some(t16846=>t16846.length>=4&&countryTokens16846.includes(t16846));\n          const relevance16846=Math.max(0,Math.min(1,Number(countryCandidate16846.relevance)||0));\n          const acceptableCountry16846=countryName16846===queryKey16846||(relevance16846>=0.85&&queryTokens16846.length===countryTokens16846.length&&sharedSubstantive16846);\n          if(acceptableCountry16846)f=countryCandidate16846;\n        }\n      }\n    }catch(_){ }\n  }\n  const zh=PLACE_ZOOM[(f.place_type&&f.place_type[0])||"place"]||15;\n'''
new='''  let f=earthlineSelectPlaceCandidate16537(q,j.features);\n  if(!f)return null;\n  /* EARTHLINE 16847 — normalized global country identity. A bare country query\n     must not be owned by an unrelated country or a street/locality that merely\n     contains the country name. Normalize accents, punctuation and an optional\n     leading article before comparing names. If the initial result is not a country\n     match, ask once for countries only. Exact names win; close exonyms may win on\n     shared substantive tokens; and a high-relevance country may replace a non-country\n     result whose normalized name contains the entire query. This keeps Cabo/Cape\n     Verde and Côte d'Ivoire/Ivory Coast valid while rejecting Chiang Mai/Mali.\n     Qualified/address searches retain the existing path. Hydrology is unchanged. */\n  const countryIdentityKey16847=v16847=>String(v16847||'').normalize('NFD').replace(/[\\u0300-\\u036f]/g,'').toLowerCase().replace(/[’'`-]+/g,' ').replace(/[^a-z0-9]+/g,' ').trim().replace(/^the\\s+/,'');\n  const bareCountryCheck16847=!/[0-9,@]/.test(String(q||''))&&countryIdentityKey16847(q).split(/\\s+/).filter(Boolean).length<=5;\n  const queryKey16847=countryIdentityKey16847(q),selectedType16847=earthlinePlaceType16537(f),selectedName16847=countryIdentityKey16847(f.text||String(f.place_name||'').split(',')[0]);\n  const initialCountryMatch16847=selectedType16847==='country'&&selectedName16847===queryKey16847;\n  const containedNonCountry16847=selectedType16847!=='country'&&(selectedName16847===queryKey16847||selectedName16847.includes(queryKey16847));\n  const wrongCountryName16847=selectedType16847==='country'&&selectedName16847!==queryKey16847;\n  if(bareCountryCheck16847&&!initialCountryMatch16847&&(containedNonCountry16847||wrongCountryName16847)){\n    try{\n      const countryUrl16847="https://api.mapbox.com/geocoding/v5/mapbox.places/"+encodeURIComponent(earthlinePlaceLookupQuery16541(q))+".json?limit=5&types=country&language=en&access_token="+MAPBOX_TOKEN;\n      const countryResponse16847=await fetch(countryUrl16847);\n      if(earthlineIsStale(myGen))return null;\n      if(countryResponse16847.ok){\n        const countryJson16847=await countryResponse16847.json();\n        if(earthlineIsStale(myGen))return null;\n        const rows16847=(countryJson16847&&countryJson16847.features||[]).filter(c16847=>earthlinePlaceType16537(c16847)==='country');\n        let countryCandidate16847=null,bestScore16847=-Infinity;\n        for(const c16847 of rows16847){\n          const names16847=[c16847.text,String(c16847.place_name||'').split(',')[0]].map(countryIdentityKey16847).filter(Boolean);\n          for(const countryName16847 of names16847){\n            const relevance16847=Math.max(0,Math.min(1,Number(c16847.relevance)||0));\n            let matchScore16847=-Infinity;\n            if(countryName16847===queryKey16847)matchScore16847=1000+relevance16847*10;\n            else{\n              const queryTokens16847=queryKey16847.split(/\\s+/).filter(Boolean),countryTokens16847=countryName16847.split(/\\s+/).filter(Boolean);\n              const shared16847=queryTokens16847.filter(t16847=>t16847.length>=4&&countryTokens16847.includes(t16847)).length;\n              if(relevance16847>=0.85&&queryTokens16847.length===countryTokens16847.length&&shared16847>0)matchScore16847=100+shared16847*10+relevance16847;\n              else if(containedNonCountry16847&&relevance16847>=0.90)matchScore16847=50+relevance16847;\n            }\n            if(matchScore16847>bestScore16847){bestScore16847=matchScore16847;countryCandidate16847=c16847;}\n          }\n        }\n        if(countryCandidate16847&&bestScore16847>-Infinity)f=countryCandidate16847;\n      }\n    }catch(_){ }\n  }\n  const zh=PLACE_ZOOM[(f.place_type&&f.place_type[0])||"place"]||15;\n'''
if old not in s:
    raise SystemExit('16847 current fallback block not found')
if 'countryIdentityKey16847' in s:
    raise SystemExit('16847 already present')
s=s.replace(old,new,1)
p.write_text(s,encoding='utf-8')
print('patched geocoder: EARTHLINE 16847 normalized global country identity')
