import {chromium} from 'playwright';
import fs from 'fs';
const src=fs.readFileSync('lab/earthline-16839-supertile-local-phase-ab.mjs','utf8');
const m=src.match(/const injection=String\.raw`([\s\S]*?)`;\n\nconst browser=/);if(!m)throw new Error('16839 injection not found');
let injection=m[1].replace('groupSize16839=4','groupSize16839=6').replace('for(const d16839 of [-.2,0,.2])','for(const d16839 of [0])').replace("grid:'24x24 grouped 4x4'","grid:'24x24 grouped 6x6; median-only cell phases'").replace("build:'EARTHLINE 16839 A/B'","build:'EARTHLINE 16841 A/B'").replace("max 36 statewide-DEM local contour windows","max 16 statewide-DEM local contour windows with one local median phase per gap plus group quantiles");
const states=['Arkansas','Iowa','Texas'],browser=await chromium.launch({headless:true}),rows=[];
for(const state of states){const page=await browser.newPage({viewport:{width:1800,height:900}});let error=null,injectError=null;await page.route('https://earthlinedevelopment.org/**',async route=>{const req=route.request();if(req.resourceType()!=='document'){await route.continue();return;}const u=new URL(req.url());if(u.pathname!=='/'&&u.pathname!=='/index.html'){await route.continue();return;}const resp=await route.fetch();let body=await resp.text(),anchor='    /* EARTHLINE 16780 — the coarse 6x6 coverage owner can pass while a large',at=body.indexOf(anchor);if(at<0){injectError='16780 anchor missing';await route.fulfill({response:resp,body});return;}body=body.slice(0,at)+injection+body.slice(at);await route.fulfill({response:resp,body});});try{await page.goto('https://earthlinedevelopment.org/?ab16841='+state+'-'+Date.now(),{waitUntil:'domcontentloaded',timeout:60000});await page.waitForSelector('#searchInput',{timeout:30000});await page.evaluate(q=>{const i=document.getElementById('searchInput'),b=document.getElementById('runBtn');i.value=q;i.dispatchEvent(new Event('input',{bubbles:true}));i.dispatchEvent(new Event('change',{bubbles:true}));b.click();},state);await page.waitForFunction(()=>{const d=window.EARTHLINE_REGIONAL_DISPLAY_AUDIT_16040||window.EARTHLINE_REGIONAL_DISPLAY_AUDIT_16020;return window.EARTHLINE_STATEWIDE_SUPERTILE_16839&&window.EARTHLINE_CORRIDOR_PUBLICATION_AUDIT_16167?.generated>0&&Number(d?.swaleLines||0)>0;},null,{timeout:110000,polling:100});await page.waitForTimeout(300);}catch(e){error=String(e?.message||e);}const snap=await page.evaluate(()=>{const p=window.EARTHLINE_CORRIDOR_PUBLICATION_AUDIT_16167||{},d=window.EARTHLINE_REGIONAL_DISPLAY_AUDIT_16040||window.EARTHLINE_REGIONAL_DISPLAY_AUDIT_16020||{},perf=window.EARTHLINE_REGIONAL_PERFORMANCE_16191||{},flow=window.EARTHLINE_LAND_VALIDITY_FLOW_AUDIT_16584||{},land=window.EARTHLINE_LAND_VALIDITY_16584||{},loc=window.EARTHLINE_STATEWIDE_SUPERTILE_16839||{};return {generated:+p.generated||0,visible:+d.swaleLines||0,unsafe:+flow.unsafeSegments||0,outside:+(p.outsideJurisdiction||p.outside||p.outsideCount||0),waterReady:Array.isArray(land.waterParts),coreMs:+perf.totalMs||NaN,localMs:+loc.elapsedMs||0,gaps:+loc.gaps||0,groups:+loc.groups||0,added:+loc.added||0,failed:+loc.failed||0,targetCandidates:loc.targetCandidates||[],groupRows:loc.groupRows||[]};});if(state==='Arkansas')await page.screenshot({path:'out/16841-arkansas.png',fullPage:false});rows.push({state,error,injectError,...snap});console.log(JSON.stringify({state,error,coreMs:snap.coreMs,localMs:snap.localMs,groups:snap.groups,added:snap.added,targetCandidates:snap.targetCandidates.length,maxLevels:Math.max(0,...snap.groupRows.map(r=>r.levels||0))}));await page.close();}
await browser.close();const pass=rows.every(r=>!r.error&&!r.injectError&&r.generated>0&&r.generated===r.visible&&r.unsafe===0&&r.outside===0&&r.waterReady&&Number.isFinite(r.coreMs)&&r.coreMs<=15000&&r.failed===0)&&rows.find(r=>r.state==='Arkansas')?.targetCandidates?.length>0;fs.mkdirSync('out',{recursive:true});fs.writeFileSync('out/16841-tristate.json',JSON.stringify({at:new Date().toISOString(),pass,rows},null,2));console.log(JSON.stringify({pass,rows:rows.map(r=>({state:r.state,coreMs:r.coreMs,localMs:r.localMs,groups:r.groups,added:r.added,targetCandidates:r.targetCandidates.length,maxLevels:Math.max(0,...r.groupRows.map(x=>x.levels||0))}))}));if(!pass)process.exit(2);
